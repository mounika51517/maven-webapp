pipeline{
    agent any
    stages{
        stage('package'){
            steps{
              def path = tool name: 'maven1', type: 'maven'
              sh script: "${path}/mvn package"
                }
        }
    }
}
