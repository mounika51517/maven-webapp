pipeline{
    agent any
    stages{
        stage('package'){
            steps{
                tools {
                 path = maven 'maven1'  
                      }
                sh script: "${path}/mvn package"
                }
        }
    }
}
